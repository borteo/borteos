.details

  %p
    If you have any questions, requests or just want to tell me Hi, please do not hesitate to send me a message.

  = form_for @contact, :remote => true do |f|
    
    .hidden
      = f.label :nickname
      = f.text_field :nickname

    .form-left
      %div
        = f.label :name, :class => "label"
        = f.text_field :name, :class => "input", 'data-validation' => "required", 'type' => "text"
      %div
        = f.label :email, :class => "label"
        = f.text_field :email, :class => "input", 'data-validation' => "required", 'data-validation' => "email", 'type' => "text"
    
    .form-right
      = f.label :message, :class => "label"
      = f.text_area :message, :class => "text-input", 'data-validation' => "required", 'type' => "text", 'rows' => 4, 'cols' => 30
    
    .form-action
      = f.submit "Send a Message", :class => "submit"

    .result